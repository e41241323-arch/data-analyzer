# TODO

## Completed / In Progress
- [x] Update chart download to generate real PNG (fix filename/format mismatch causing "line-chart.png could not be opened").


## Next steps
- [ ] After code changes: run `npm run lint` and `npm run build` to ensure Vercel deployment compatibility.

